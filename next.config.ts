import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Vazio de propósito. Cada opção aqui é uma coisa a mais que pode
  // quebrar numa atualização; só entra o que a aplicação provar precisar.
}

export default nextConfig
